
export const appTheme = {
    colorScheme: 'dark',
    primaryColor: 'green',
    Textarea: (theme) => ({
        root: {
            backgroundColor: 'red',
            borderRadius: '0px',
        }
    })
}